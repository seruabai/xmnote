# 改动记录

> 记录每次实质性改动，供所有 AI 接续时了解最新状态。
> **规则**：完成一次改动（或一批关联改动）后在顶部追加一行：日期 / 工具 / 改动摘要 / 涉及文件。

| 日期 | 工具 | 改动摘要 | 涉及文件 |
|------|------|----------|----------|
| 2026-09-17 | DeepSeek Harness | **第五批：点「完成」抽动修复 + 主题换成小米笔记的米黄纸**。①[侧栏]**点「完成」时界面抽动**：根因是"两次位移叠加"——收起键盘让窗口重排、把焦点行自动滚进可视区，列表又在保存完成后异步重建，而滚动恢复写在 `scroll.post{}` 里，于是先画出一帧"滚动在 0"的内容、下一帧再跳回原偏移。改法：点"完成"那一刻把滚动位置钉住（`pinnedScrollY`），换取装用 `OnGlobalLayoutListener` 在**第一次布局里**（早于绘制）就恢复到该位置，并取消 350ms 防抖中的那次保存，重绘不再往后推。**逐帧验收**（screenrecord 60fps + 逐帧像素位移分析）：修复前同场景有 2 次位移（+31px、−70px），修复后 **0 次**，换装后两次采样位置完全一致（marker y 279→279）。②[全局]**主题换成小米笔记的米黄纸**：色值全部取自 MIUI 笔记 2.3.2.7 反编译 `res/values/colors.xml` —— `paper_yellow #FFFAF0` 做全屏底色、`paper_white #FFFFFF` 做卡片、`miuix_color_yellow_light_primary_default #FFB21D` 做强调色（FAB/选中/“完成”键）、`#FFF0D3`/`#FFF5EA` 做浅黄容器、`#9D6802` 做暖棕次要文字；深色主题收口成暖黑 `#17140E` + 同色系黄。XML 主题的 `windowBackground`/闪屏同步换色，侧栏悬浮层（卡片/胶囊/文字/“完成”键）也换到同一套色。**像素验收**：首页/待办/设置底色 = (255,250,240)、卡片 = (255,255,255)、深色底色 = (23,20,14)。③工具：新增 `tools/verify/theme_verify.py`（抓屏取像素核色）；`sidebar_verify.py` 修正把手定位来源——此前用 `dumpsys window` 的 mAttrs，它和真实 frame 不同步（attrs 说 y=816、真实在 y=944），"注入拉不出面板"是误判，改用 `dumpsys input` 的 frame 后**点按把手即可开面板**，并新增 S4（点完成后不抽动、面板不连带收起）。**未完成**：④「隐藏保活通知」在 AOSP 13 模拟器上不生效——`nm.cancel` 四拍调用全部成功（logcat 过滤 `QuickCapture` 可见 4 条），但系统在服务仍处于前台态时会把前台通知重新挂回，`NotificationRecord id=42` 仍在；需要允许撤回前台通知的机型（部分 MIUI/HyperOS）才会 PASS，待真机确认 | ui/theme/Theme.kt, notify/QuickCaptureService.kt, res/values/themes.xml, res/values-v31/themes.xml, res/values-night/themes.xml, res/values-night-v31/themes.xml, tools/verify/sidebar_verify.py, tools/verify/theme_verify.py, wiki/* |
| 2026-09-17 | DeepSeek Harness | **第四批反馈（部分完成）**。①[全局]**勾选栏与文字中线对齐**：汉字墨水中心比行盒中心低，居中摆放的方框看着"偏高"；给 `MiCheckbox` 与块编辑器的 `Checkbox` 加**按尺寸比例的光学下移**（7%，随字号缩放故三档文字大小都成立），并把待办行文字样式收紧（`includeFontPadding=false` + `LineHeightStyle(Center, Trim.Both)`）。**像素测量验收**（框描边中心 vs 同行文字墨水中心）：清单编辑器偏差 +1px、待办卡片 +0.5px（改前分别是 -6px / -6.4px）。②[侧栏]回车不再收起输入法：单行+`IME_ACTION_NEXT` 换成多行+`IME_FLAG_NO_ENTER_ACTION`，回车由 `setOnKeyListener` 接住插新行；**按用户要求保留回滚**——`ENTER_KEEPS_KEYBOARD` 常量置 false 即回到旧行为，两条分支代码都留着。③[侧栏]点开**单条待办**编辑：原文落到内容行、标题行留空显示占位"待办清单"、光标落到第三行空白行（原来是把原文当标题）。④[侧栏]新增"隐藏保活通知"开关：`startForeground` 挂上后立刻 `cancel`，消息栏不留条目，设置页可关闭该行为（已在设置弹窗里确认两个开关都在）。**未完成**：②③④的设备验收——侧栏面板是 `TYPE_APPLICATION_OVERLAY` 的裸 View，无无障碍节点且注入点按/滑动都未能拉出面板，需要真机手动确认；①"点完成时界面抽动"尚未修 | ui/TodoCard.kt, ui/NoteBlockBody.kt, notify/QuickCaptureService.kt, ui/SettingsScreen.kt, tools/verify/sidebar_verify.py, wiki/* |
| 2026-09-17 | DeepSeek Harness | **待办页两条反馈修复**。①[待办]左滑露删除键时**勾选栏直接消失**（`RevealDeleteRow` 把滑动进度交给内容，滑动超过阈值就收起勾选框；主条目与子条目都收起）——此前勾选框被卡片一起拖走，看着像要被删掉。②[待办]多选态的两个按钮按用户要求挪位：**退出多选的 X 移到"待办"大字上方那一行**，右上角**由设置齿轮换成全选**（退出多选恢复齿轮）；"已选 N 项"改挂在标题右侧。为此把 `TodoHeader` 改成选择态感知并移入 `TodoPane` 渲染，选中集合不必提升到 HomeScreen。顺带补上齿轮的 `contentDescription="设置"`（自绘 Canvas 此前在无障碍树里没有身份）。**设备验证**：新增 `todo_feedback_verify.py` **10/10 PASS**（左滑后同行勾选框 0 个 + 删除键冒出来；退出键 y=139..265 在大字 y=281 上方；全选 x=938..1064 在右上；多选时齿轮为 None、退出后回来） | ui/TodoSwipe.kt, ui/TodoCard.kt, ui/TodoPane.kt, ui/HomeScreen.kt, tools/verify/todo_feedback_verify.py, wiki/* |
| 2026-09-17 | DeepSeek Harness | **用户四条反馈修复（小米 12S 体验反馈）**。①[编辑器]保存状态从正文流里挪出来，改**右下角悬浮**（原来"待保存"出现/消失会把整篇内容顶一下，用户反馈"点一下勾选框整个内容跟着动"）。②[清单]回车改为**新开一条勾选栏**（新条目未勾选、光标自动跟过去），不再是在同一条目内换行（那会渲染成"一个勾选框后面两行"）。③[清单]补上条目输入框的焦点上报：此前只有标题栏与正文块会上报，清单笔记在 API<35 上呼不出五键工具栏（`isImeVisible` 恒 false 时就靠这个信号）；同时工具栏不再限定文本笔记，"勾选"键在清单里 = 再加一条勾选栏，图片/录音/手写落到附件条。④[首页]宫格从"左右各一个 LazyColumn（瀑布流分列）"换成 **LazyVerticalGrid 单页滚动**：此前两半各滚各的，用户要求"下滑一次整页一起动"；代价是同排卡片等高。**设备验证**：新增 `tools/verify/feedback_verify.py` 11/11 PASS（含"点勾选框后条目 y 坐标不变""只滑右半屏左半屏同步位移 Δ-209/Δ-281""回车后库内变 买牛奶/9/交水费/取快递"） | ui/EditorScreen.kt, ui/HomeScreen.kt, tools/verify/feedback_verify.py, wiki/* |
| 2026-09-17 | DeepSeek Harness | **v1.2.22（versionCode 25）**：正文编辑器整体换成块模型（body_format_version=3、编辑器 −660 行、标记桥删除）+ 思维笔记（脑图）首批可用 + 三处真 bug 修复（Android 13 工具栏不可达 / MiCheckbox 无障碍语义 / 脑图 kind 被降级）+ 取消文本块拖拽。单测 298 全绿、lintDebug 通过；设备端 Android 13 与 16 各 5 个套件合计 83 项检查 0 失败；新增按机型出报告的一键验收入口 | app/build.gradle.kts, wiki/CHANGE_ARCHIVE.md, wiki/CHANGELOG.md, tools/verify/* |
| 2026-09-17 | DeepSeek Harness | **取消文本块拖拽 + 修三个真 bug + 大数据量直连验收**。①**按用户要求取消文本块拖拽**：只有无输入框的块（图片/录音/链接卡片）可长按拖动，文本块长按回归选字——避免"边想边点屏幕"时误触发排序；附带把系统那个 `Select all / Autofill` 浮层彻底消灭（它只在长按文本框起拖时出现）。新增 `RichBlock.draggable`，`beginDrag` 命中文本块直接返回（不吞事件、不给触感）。②**修 API<35 工具栏不显示**：`adjustResize` 下窗口被压缩、`WindowInsets.isImeVisible` 恒为 false，Android 13 上样式键完全点不到；改为并上"正文/标题获得焦点"这一路信号，**API 33 上样式键验收从不可达变成 10/10 PASS**。③**修 `MiCheckbox` 无障碍缺陷**：清单编辑器与待办卡片共用的自绘勾选框只有 `clickable`，没有 role/checked 语义（读屏念不出"勾选框、勾没勾"，设备也定位不到），改用 `toggleable(role = Role.Checkbox)`。④**新增 `tools/verify/scale_verify.py`（大数据量纯直连验收，全程不截图）**：A 块渲染矩阵 15 块逐块核对、B 文本块不可拖反向断言、C 6 个样式键 × 整篇 6 块字段 diff、D 10 条清单 UI 勾选核对、E 脑图 6 节点几何/折叠/展开、F 崩溃扫描 + `PRAGMA integrity_check` + 内存快照；**Android 13 与 Android 16 两台各 42 项检查 0 失败**，明细落 `logs/scale-report-*.json`。另把拖拽验收脚本改成新口径（文本不可拖 + 图片可拖，6/6 PASS）。**排查掉 4 个假阳性并写清成因**：回车后打字失败是 `input text` 送字母/中文在 API 36 镜像上进不去（数字能进）、`focused` 属性不是 Compose 虚拟焦点的可靠信号、脑图卡片按标题找不到（清单卡片标题位显示的是进度）、连续拖拽偶发不触发（应用侧连 arm 日志都没有，是注入撞长按 500ms 窗口）——都不是应用缺陷，脚本里已记录并各自规避 | ui/NoteBlockBody.kt, ui/EditorScreen.kt, ui/TodoCard.kt, core/RichDoc.kt, tools/verify/scale_verify.py, tools/verify/block_drag_verify.py, tools/verify/block_edit_verify.py, wiki/* |
| 2026-09-16 | DeepSeek Harness | **脑图拖动换父 + 视图细节（任务④第二批）**。画布上长按节点拖动即可换父：长按前不碰事件、越过 slop 才接管（与正文块拖拽同一套），命中谁就以谁为新父，拖到自己子孙上由 `MindNode.move` 的环检测挡下（树原样不动）；换父后新父自动展开，否则用户看不到结果。顺带补上脑图的两处投影：字数改数节点文字（`charCount`），分享改成缩进大纲（原来会分享出空内容）。**设备验证 `mind_verify.py` 19/19 PASS**（新增：拖子二到子一上 → 库内变成子一的子节点、根下不再挂它；拖到自己的子孙上 → 树逐字节不变）；块编辑回归 5/5、单测 298 全绿 | ui/MindEditor.kt, ui/EditorScreen.kt, tools/verify/mind_verify.py, wiki/* |
| 2026-09-16 | DeepSeek Harness | **脑图接入数据层与界面（任务④第一批）**。新增 `NoteKind.MIND`（落库 kind=2，`storedCode()/noteKindOf()` 收敛编码）与 `Note.mind: MindDoc?`；`NoteRepository` 按 kind 分派：脑图正文走 `MindCodec`（**不进块文档闸口**），卡片的 `body` 投影成大纲文字；`SaveCommand/SaveCoordinator/NoteViewModel` 全程带 `mind`。新增 `ui/MindEditor.kt`：导图视图（`MindLayout` 的整齐树 + 三次贝塞尔连线，**节点是真实 Composable 而不是画在 Canvas 上**——否则无障碍树里什么都没有，读屏念不出、设备也测不到）、大纲视图（缩进树 + 折叠）、节点操作栏（加子级/加同级/改文字/折叠展开/删除）、节点文字对话框、点空白取消选中；首页新增「新建脑图」小按钮（主 FAB 的"一下即新笔记"不动）。脑图没有独立标题：根节点文字即标题（`MindDoc.displayTitle`）。**过程中抓到并修掉两个会坏数据的自身 bug**：①`NoteRepository.saveExisting` 仍按 0/1 写 kind → 保存一次脑图就被降级成文本笔记（设备脚本断言 kind=2 时抓到）；②备份导入把脑图正文过了 `NoteBody` 闸口 → 整棵树会被当成 Markdown 压成一行。新增 `NoteKindCodeTest`（锁死编码映射）。**设备验证 `tools/verify/mind_verify.py` 15/15 PASS**：渲染与几何（子节点在父节点右侧、兄弟不重叠）、加子级落库、折叠落库且孙节点从导图消失、大纲视图、首页新建脑图全链路（kind=2 落库）；另三套脚本回归全绿（拖拽 6/6、块编辑 5/5、工具栏 10/10）。单测 298 全绿 | data/Models.kt, data/Db.kt, data/NoteRepository.kt, backup/BackupCodec.kt, feature/notes/EditorReducer.kt, feature/notes/SaveCoordinator.kt, NoteViewModel.kt, ui/MindEditor.kt, ui/EditorScreen.kt, ui/HomeScreen.kt, ui/NoteCard.kt, app/src/test/.../data/NoteKindCodeTest.kt, tools/verify/mind_verify.py, wiki/* |
| 2026-09-16 | DeepSeek Harness | **工具栏切成"直接操作光标所在块"，标记桥删除（任务②-2.4）**。正文状态从"一段 Markdown 文本"换成 **块文档**：`EditorScreen` 持 `doc: RichDoc`、`NoteBlockBody` 契约改为 `doc/onDocChange/BlockCursor`，`Note` 模型新增 `doc`，`NoteRepository/SaveCommand/NoteViewModel` 全程传文档（写入用 `NoteBody.encode(doc)`，清单笔记仍由 `LegacyBody.fromChecklist` 转换）。样式键（H1-3/•/1./❝/首缩/尾缩/勾选）改为改块属性：新增 `RichDoc.toggleHeadTag(blockId, type, ordered)`（同种→取消、异种→替换，**ITEM 靠 number 区分 • 与 1.**，有序接续上块序号+1 与旧行为一致）。**删除**：`TextNoteBody`、`MarkupOverlay`、`transformNoteText`、`enterInheritIntercept`、`backspaceIntercept`、`NoteMarkupVisualTransformation`、`insertEmbedMarkup` 及全局偏移三件套（`lineStarts/blockIndexAtOffset/offsetIn`）与它们的 174 行单测——编辑器不再经过标记文本往返。顺带把字数改成只数用户写的字（行首标记不再计入）。**设备验证**：API 36 AVD `tools/verify/block_toolbar_verify.py` **10/10 PASS**（光标停在 BBB 上按 H1/•/1./❝/勾选/首缩，逐项核对库内**只有 BBB 这一块**变了、文字不变、另两块不受影响）；API 33 AVD `block_edit_verify.py` **5/5 PASS**（打字落库、回车拆块、段首退格并块、勾选框翻转）；2.3 的拖拽脚本回归 **6/6 PASS**。单测 296 全绿 | NoteViewModel.kt, core/BlockEdit.kt, core/RichDoc.kt, data/Models.kt, data/NoteRepository.kt, feature/notes/EditorReducer.kt, feature/notes/SaveCoordinator.kt, ui/EditorScreen.kt, ui/NoteBlockBody.kt, tools/verify/block_toolbar_verify.py, tools/verify/block_edit_verify.py, wiki/* |
| 2026-09-16 | DeepSeek Harness | **块级长按拖拽排序（任务②-2.3）**。新增 `core/BlockDrag.kt`（纯函数 + 13 项单测）：长按命中判定、指针纵坐标→插入边界（按悬停块上/下半区决定插前插后）、边界→`RichDoc.move` 目标下标（顺序不变返回 null，调用方不写库也不画线）。`ui/NoteBlockBody.kt` 接入：懒列表外包 Box 画插入指示线；长按用 `awaitLongPressOrCancellation`，**长按前不碰事件**（文本框点击/选区、勾选框、滚动照常），越过 `touchSlop` 才进拖拽并在 Initial 阶段吃掉事件；被拖块 `graphicsLayer` 抬起跟手 + 触感反馈，上下边缘自动滚动。**与小米的有意差异**：小米一律插到悬停块之后（第一块永远拖不到文首），这里按悬停块上下半区决定插前/插后。**实测抓出并修掉两个自身 bug**：①用单次事件位移和 slop 比较，慢速滑动（每条事件零点几像素）拖拽永远起不来；②自己累加增量会丢位移，改指针绝对位置后落点才准（此前差一格）。**设备验证 `tools/verify/block_drag_verify.py` 6/6 PASS**：界面顺序 CCC/AAA/BBB + 库内 v3 块顺序一致 + 原位拖动不写库。单测 309 全绿。遗留：文本框长按选字与块拖拽抢同一手势，从文字上起拖会带出系统 Select all/Autofill 浮层（拖拽结束时选区已收回） | core/BlockDrag.kt, ui/NoteBlockBody.kt, tools/verify/block_drag_verify.py, app/src/test/.../core/BlockDragTest.kt, wiki/TASK_PROGRESS.md, wiki/CHANGELOG.md |
| 2026-09-16 | DeepSeek Harness | **图片/录音改为真正的块插入（任务②-2.2）**。新增 `core/BlockEdit.kt`：全局偏移↔块映射、`insertEmbedAt`（空行就地占位，沿用旧行为；插入后其后必有可输入文本块——块编辑器里嵌入块没有输入框，否则光标无处可去）、`insertEmbedMarkup`。`EditorScreen` 四处入口（拍照/相册/录音/手写）统一走 `insertEmbedAsBlock()`，按块计算插入位置并把光标移到新落点，不再拼 `![](...)` 文本行。顺带修掉一处无障碍瑕疵：`NoteBlockBody` 里 alpha=0 的提示文字看不见却仍进语义树，已删除。单测新增 12（全量 296 全绿）。**设备验证（adb 直连）**：造含 IMAGE 块的 v3 笔记 → 卡片显示「［图片］」→ 图片块渲染 964x840 且下方存在输入块 → 输入 after-img → 库内 `[TEXT]/[IMAGE]/[TEXT]` 三块结构正确 | core/BlockEdit.kt, core/LegacyBody.kt, ui/EditorScreen.kt, ui/NoteBlockBody.kt, app/src/test/.../core/BlockEditTest.kt, wiki/TASK_PROGRESS.md, wiki/CHANGELOG.md |
| 2026-09-16 | DeepSeek Harness | **正文块渲染（任务②-2.1）**。新增 `ui/NoteBlockBody.kt`：与旧 `TextNoteBody` **参数契约完全一致**，`EditorScreen` 仅换一行调用，保存/工具栏/插图/IME 逻辑均不动、出问题一行回退。渲染改为块列表（H1-3 字号 / TODO 勾选框 / •与 n. 列表 / 引用 / 首行缩进 / 图片块按需降采样 / 录音块 / 链接块）；交互实现回车拆块并按类型续接、段首退格并块、清空并入上一块、新块 requestFocus。开发中实测发现并修掉两个自身 bug：输入未同步文档导致回车拆块错位、FocusRequester 从未真正请求焦点。**设备验证走 adb 直连**（emulator-5554 + uiautomator dump + sqlite3）：新建笔记输入 alpha→回车→beta 后界面呈现两个独立块、字数 10 字，库内该行为 kind=0 / body_format_version=3 / 两个 TEXT 块。旧 `TextNoteBody` 暂留作回退参考 | ui/NoteBlockBody.kt, ui/EditorScreen.kt, wiki/TASK_PROGRESS.md, wiki/CHANGELOG.md |
| 2026-09-16 | DeepSeek Harness | **富文本编辑器第二批：数据库迁移到 v3 块文档**。`DB_VERSION` 9→10，新增 `< 10` 分支把正文逐行转为块文档（单行转换失败即跳过、保持旧版本号，读取端按行内版本解析，所以「没迁移成功」只是没升级、不会显示为空）；写入路径改为**显式**写 `body_format_version`（此前依赖 DDL 默认值，正是漏标版本的根源），全新安装默认值同步为 3；新增唯一格式闸口 `core/NoteBody.kt`：按版本解码 + 双向格式嗅探（v3 内容标成 v2 也能认出来；v2 内容标成 v3 会回退按标记解析）+ v1 私有标记先过 `migrateBodyV1toV2`；`NoteRepository` 读写与 `BackupCodec` 导入统一走该闸口。设备测试同步更新三条既有断言（v8→v10 / v1→v10 / 全新安装）并新增 v9→v10 专项用例（文本、清单、已升级三类行 + 重复打开幂等）。**单测 284 全绿；androidTest 仅编译通过、未在设备运行**。遗留：旧版 APK 读不了新包，兼容与否待定 | data/Db.kt, core/NoteBody.kt, data/NoteRepository.kt, backup/BackupCodec.kt, app/src/test/.../core/NoteBodyTest.kt, app/src/androidTest/.../data/SchemaMigrationTest.kt, wiki/TASK_PROGRESS.md, wiki/CHANGELOG.md |
| 2026-09-16 | DeepSeek Harness | **块文档迁移的设备级验证（直连模拟器，非截图）**。用户决定「当新程序做、不背历史数据包袱」，故备份跨版本兼容不再处理。设备验证：`am instrument` 直连 emulator-5554（SDK 33）跑 `SchemaMigrationTest` **4/4 OK**（v8→v10、v1→v10、全新安装、v9→v10 幂等）；新增 `BodyFormatWritePathTest` **3/3 OK**，它在设备上直接以只读连接打开数据库文件读原始行，断言：文本笔记库内是 v3 块文档 JSON（而非标记文本）且读回与写入逐字一致、清单笔记落成 TODO 块且勾选状态保留、修改路径 `saveExisting` 同样写 v3。另用 `sqlite3` 直查真机库确认 `user_version=10`、`body_format_version DEFAULT 3`、14 张表齐全。已知环境噪声：模拟器出现 System UI ANR（swiftshader 软渲染），与 App 无关，App 进程存活 | app/src/androidTest/.../data/BodyFormatWritePathTest.kt, wiki/TASK_PROGRESS.md, wiki/CHANGELOG.md |
| 2026-09-16 | DeepSeek Harness | **富文本编辑器升级第一批：块模型内核**。用户确认收掉 T-006-C（此前只按截图借鉴、效果差，改为对着反编译实现写）。借鉴小米笔记富文本编辑器的扁平块定义（text/todo/item/image/sound/link + fragments 行内片段 + checked/sizeLevel/textAlign）与其 note-parser 的解析-序列化-自愈结构，代码为 Kotlin 全新实现，不含任何小米代码或资源。新增 `core/RichDoc.kt`（块模型 + 纯函数操作：插入/删除/拖拽排序/拆块/并块/行内样式区间开关/清单进度/标题推导）、`core/RichDocCodec.kt`、`core/LegacyBody.kt`（旧标记文本 ↔ 块文档双向无损，解析直接复用 NoteMarkup 以保证与线上渲染逐字一致；回退时旧格式表达不了的样式丢弃、文字保留）。**未动数据库与 UI**：body_format_version 仍为 2，迁移方案（用户选定一次性全库迁移到 v3）待下一步实施。单测新增 36，全量 267 全绿 | core/RichDoc.kt, core/RichDocCodec.kt, core/LegacyBody.kt, app/src/test/.../core/*, wiki/TASK_PROGRESS.md, wiki/CHANGELOG.md |
| 2026-09-16 | DeepSeek Harness | **脑图（思维笔记）第一批：纯逻辑层**。借鉴小米笔记思维笔记的节点模型与交互语义（导图/大纲双视图、插入子级/同级/父级、拖拽换父、折叠角标），**代码为 Kotlin 全新实现，不含任何小米代码或资源**。新增 `feature/mind`：`MindMap.kt`（不可变树 + 15 项操作，含成环检测）、`MindCodec.kt`（kotlinx.serialization 容错编解码，坏数据返回 null 不抛异常）、`MindLayout.kt`（分层整齐树布局：同层左对齐成列、兄弟子树带不重叠、父节点居中于子节点块、折叠整支裁剪、中日韩全角宽度测算与折行）。JVM 单测新增 33 全绿（`:app:testDebugUnitTest --tests "com.purenote.local.feature.mind.*"`）。**尚未接线**：NoteKind.MIND、编辑器入口未动；`kind` 列已存在故无需数据库迁移 | feature/mind/MindMap.kt, feature/mind/MindCodec.kt, feature/mind/MindLayout.kt, app/src/test/.../feature/mind/*, wiki/TASK_PROGRESS.md, wiki/CHANGELOG.md |
| 2026-09-16 | DeepSeek Harness | **v1.2.21（versionCode 24）发布**：随本版首次对外交付《规范》A0–G 数据可靠性重构 + H 云能力。版本号与存档见 `wiki/CHANGE_ARCHIVE.md`；发布走 tag `v1.2.21` 触发 release.yml | app/build.gradle.kts, wiki/CHANGE_ARCHIVE.md, wiki/CHANGELOG.md |
| 2026-09-16 | DeepSeek Harness | **规范阶段 H（云能力）**：按用户 2026-09-15 的产品定位调整实装 `INTERNET`/`ACCESS_NETWORK_STATE`（只在用户开启并点「立即同步」时联网）。新增 `sync/`：`RemoteTransport` 抽象 + `WebDavTransport`（手写 PROPFIND/MKCOL/GET/PUT/DELETE + 无依赖解析 207）+ `AndroidCredentialStore`（AndroidKeyStore + AES-GCM，不降级明文）+ `BackupSyncEngine`（导出→本地清单校验→上传→**读回远端整包校验 backupId/大小**→才记录成功）+ 设置页「云同步」栏。凭据与配置分离存储，明文 HTTP 只对模拟器宿主机与回环开放。JVM 单测 198（新增 34：路径/207 解析/MockWebServer 协议/引擎顺序）；设备端到端：真实 wsgidav 服务器，文件落盘 + 读回校验。**首版只上传完整备份包，不做双向同步、不删远端旧包** | AndroidManifest.xml, res/xml/network_security_config.xml, sync/*, backup/BackupIo.kt（inspect）, data/NoteRepository.kt, NoteViewModel.kt, ui/SettingsScreen.kt, ui/CloudSyncSection.kt, gradle/libs.versions.toml, app/build.gradle.kts, app/src/test/.../sync/*, app/src/androidTest/.../sync/CloudSyncWebDavTest.kt, tools/verify/cloud_sync_webdav.ps1, wiki/SPEC_REBUILD_MAPPING.md, wiki/DECISIONS.md, AGENTS.md, README.md |
| 2026-09-15 | DeepSeek Harness | **规范实施 A0–G**：按《PureNote 技术选型、实现规范与验收方案》完成 12 个提交（分支 `spec/rebuild-a-h`）。A0 修 2 个砖化 bug（超长序号令首页崩溃 / 升级锁库）+ 插图吞行 + 图片标记被拆碎 + O(n²)；A 保全型损坏处理器 / 停用未保护清理；B 统一事务入口 + 显式 SaveResult；C **v9 加法迁移** + CAS + 幂等 + 历史快照 + body_format_version；D 编辑状态机 + 保存协调器 + 不可变附件；E 备份完整性清单 + 一致快照 + SAF 读回验证；F 活动存储指针 + 按代恢复 + 旧写入隔离；G WorkManager 自动备份 + 保留策略 + 异常删改保护。单测 164、设备 48、崩溃注入 6 次强杀全绿 | data/, data/store/, feature/notes/, feature/backup/, platform/backup/, backup/, core/NoteText.kt, core/ImageStore.kt, ui/EditorScreen.kt, ui/SettingsScreen.kt, NoteViewModel.kt, PureNoteApp.kt, notify/ReminderReceiver.kt, gradle/libs.versions.toml, app/build.gradle.kts, tools/verify/crash_injection.ps1, wiki/SPEC_REBUILD_MAPPING.md, wiki/SPEC_REBUILD_REPORT.md |
| 2026-09-13 | ZCode | [侧栏]待办新增先只显示内容行、回车后才出现标题行（OverlayTodoDraft.titleRevealed）；[侧栏]编辑中点空白改保存退回列表（面板保持拉出，不再整个收起到桌面；非编辑态点空白仍收起）；[编辑器]正文 BasicTextField 补 textStyle=bodyTextStyle，输入文字与"开始书写或"占位字号/位置对齐 | notify/QuickCaptureService.kt, ui/EditorScreen.kt |
| 2026-09-13 | ZCode | [备份]任务A UI 接线：设置页新增"备份与恢复"栏，SAF 导出（CreateDocument，cacheDir 中转）/导入（OpenDocument，预览确认后写库），NoteViewModel 新增 BackupState（Idle/Running/Done/Failed）与 export/preview/import，坏包给可读错误不崩溃；模拟器换机往返/重复导入幂等/integrity_check 全过 | NoteViewModel.kt, ui/SettingsScreen.kt, core/DateFormats.kt |
| 2026-09-13 | ZCode | [备份]新增整库导出/导入（zip：backup.json + 附件），跨设备 uuid 重映射、LWW 合并、单事务应用、Zip Slip 防护；新增 repository 三入口；新增 kotlinx-serialization 依赖；写 GLM 任务书 | backup/*, data/NoteRepository.kt, data/Db.kt, wiki/GLM_TASK_BACKUP_SYNC.md, wiki/SYNC_DESIGN.md, gradle/libs.versions.toml |
| 2026-09-13 | ZCode | [结构整理]日期格式化收敛到 core/DateFormats（三处重复→一处，SimpleDateFormat 改按线程各持一份修正线程安全隐患）；修 build.gradle.kts 被编码损坏的注释 | core/DateFormats.kt, core/TodoDates.kt, ui/Common.kt, ui/EditorScreen.kt, notify/QuickCaptureService.kt, app/build.gradle.kts, test/.../DateFormatsTest.kt |
| 2026-09-13 | ZCode | [性能]笔记列表瀑布流分列改 remember 缓存（原来每次重组都按 body.length 重算全部分列）；卡片分类名查找改预建 map；搜索加 250ms 防抖；DB v7 加 (trashed,updated_at)/(trashed,folder_id)/(trashed,parent_id) 复合索引。实测：2 万条笔记下列表查询<1ms、启动耗时与空库相当，DB 非瓶颈 | ui/HomeScreen.kt, NoteViewModel.kt, data/Db.kt |
| 2026-09-08 | Codex | [协作]改按需读取和显式发版；[CI]修复 gradlew 执行权限错误，增加原标签补附件及产物验证（应用版本不变） | AGENTS.md, .agents/skills/pre-work-check/SKILL.md, .github/workflows/release.yml, wiki/* |
| 2026-09-07 | ZCode | **v1.2.19**：[待办弹窗]删M3 ModalBottomSheet独立滑入（弹窗先出被键盘盖住再跳），换自绘Dialog弹层imePadding实时贴键盘顶沿；[多选]全选图标换FactCheck对齐实机 | ui/TodoEditSheet.kt, ui/TodoPane.kt, app/build.gradle.kts |
| 2026-09-07 | ZCode | **v1.2.18**：[同步地基]新增uuid全局ID：DB v5（notes/todos加列+存量回填+索引），Model暴露uuid；新增wiki/SYNC_DESIGN.md同步方案设计 | data/Db.kt, data/Models.kt, data/NoteRepository.kt, wiki/SYNC_DESIGN.md, app/build.gradle.kts |
| 2026-09-07 | ZCode | **v1.2.17**：[多选]删双横线+米黄，换三条杠+浅灰选中底（对标小米实机）；[侧栏]把手删浅灰75%，换深灰50%；[侧栏]删点空白仅退回列表，换保存草稿并收起整个侧栏 | ui/TodoCard.kt, notify/QuickCaptureService.kt, app/build.gradle.kts |
| 2026-09-07 | ZCode | **v1.2.16**：[测试修复]Android16实测修8处bug（弹窗闪退/侧栏新建待办无编辑卡/速记开关不持久/输入回滚/多选FAB卡死/拖拽闪回/通知id错/相对时间不刷新） | ui/TodoEditSheet.kt, ui/TodoPane.kt, ui/TodoSwipe.kt, ui/TodoCard.kt, notify/QuickCaptureService.kt, notify/ReminderReceiver.kt, notify/ReminderScheduler.kt, MainActivity.kt, ui/SettingsScreen.kt, app/build.gradle.kts |
| 2026-09-06 | opencode | **v1.2.15**：[自查返工]修复收面板键盘空转；暗罩加深到50%+无虚化补偿70%；选中米黄加深；手柄加深 | notify/QuickCaptureService.kt, ui/TodoCard.kt, app/build.gradle.kts |
| 2026-09-06 | opencode | **v1.2.14**：[侧栏]删逐帧毛玻璃虚化换全屏均匀暗罩；把手换浅灰；待办编辑换卡片原地展开+空白退回列表；[待办多选]换图2样式双横线+米黄选中 | notify/QuickCaptureService.kt, ui/TodoCard.kt, ui/TodoPane.kt, app/build.gradle.kts |
| 2026-09-06 | opencode | **v1.2.13**：[待办弹窗]修复弹窗键盘三段跳动改同动升降 | ui/TodoEditSheet.kt, app/src/main/AndroidManifest.xml, app/build.gradle.kts |
| 2026-09-06 | opencode | **v1.2.12**：待办弹窗修复点行光标落行末；侧栏编辑修复新页面改行末聚焦+完成退出；待办新增长按多选+拖柄排序；通知压单条静默+提醒id错开42；虚化半径加大去纹路 | ui/TodoEditSheet.kt, ui/TodoCard.kt, ui/TodoPane.kt, ui/HomeScreen.kt, NoteViewModel.kt, data/Db.kt, data/NoteRepository.kt, notify/QuickCaptureService.kt, notify/ReminderReceiver.kt, app/build.gradle.kts |
| 2026-09-06 | opencode | **v1.2.11**：滑动删除键改等高对齐；废纸篓删清空键时间改日期时间；前台通知去时间戳 | ui/TodoSwipe.kt, ui/TrashScreen.kt, ui/Common.kt, notify/QuickCaptureService.kt, app/build.gradle.kts |
| 2026-09-06 | opencode | **v1.2.10**：待办编辑换底部弹窗（单条回车转清单+行行回车加行）；左滑换部分露出圆形红删除键；废纸篓卡片删按钮只留标题时间+左侧勾选；设置最近删除置顶；虚化半径加大去纹路；TodoScreen拆5文件 | ui/TodoPane.kt, ui/TodoCard.kt, ui/TodoEditSheet.kt, ui/TodoSwipe.kt, ui/TodoRemindDialog.kt, ui/TrashScreen.kt, ui/SettingsScreen.kt, ui/AppRoot.kt, NoteViewModel.kt, notify/QuickCaptureService.kt, app/build.gradle.kts |
| 2026-09-06 | opencode | **v1.2.9**：待办左滑删直接删除换停留露出删除按钮；待办表新增废纸篓字段待办删除进废纸篓；废纸篓新增待办分区恢复/彻底删除/多选；行内回车新增子行自动聚焦 | ui/TodoScreen.kt, ui/TrashScreen.kt, data/Db.kt, data/NoteRepository.kt, data/Models.kt, NoteViewModel.kt, PureNoteApp.kt, app/build.gradle.kts |
| 2026-09-06 | opencode | **v1.2.8**：侧栏把手改白色75%不透明度；虚化背景去纹路（backdrop固定透明+降噪半径）；待办行内编辑光标定位末尾+回车添加子待办行；左滑直接删除（未做成划出按钮，见v1.2.9），禁用右滑完成；废纸篓支持长按多选+全选/恢复/彻底删除批量操作；设置页新增「最近删除」入口 | notify/QuickCaptureService.kt, ui/TodoScreen.kt, ui/TrashScreen.kt, ui/SettingsScreen.kt, app/build.gradle.kts |
| 2026-09-05 | opencode | **v1.2.7**：笔记页新增「新建分类」入口；侧栏把手改白色半透明并缩矮、背景改为纯模糊；把手拖拽跟手拉出面板；待办点击文字行内直接编辑（底栏：提醒时间+完成）；提醒选择器改用 Material3 日期/时间控件并将「到期日/时刻」改「日期/时间」 | app/build.gradle.kts, ui/HomeScreen.kt, ui/TodoScreen.kt, notify/QuickCaptureService.kt |
| 2026-09-05 | opencode | **v1.2.6**：全新 UI 骨架——奶油暖黄主题 + 屏幕转场动画；versionCode 9 | app/build.gradle.kts, ui/AppRoot.kt, ui/Common.kt, ui/theme/Theme.kt |
| 2026-09-05 | opencode | 变更版本策略：每次代码改动自动升 patch 版本并打 tag（规范化） | AGENTS.md, .agents/skills/pre-work-check/SKILL.md |
| 2026-09-05 | opencode | 提交全新 UI 骨架：奶油暖黄主题 + 屏幕转场动画（4b2f06a） | app/src/main/java/com/purenote/local/ui/AppRoot.kt, Common.kt, theme/Theme.kt |
| 2026-09-05 | opencode | 初始化多AI协作规范 | AGENTS.md, .agents/skills/pre-work-check/SKILL.md, wiki/* |
