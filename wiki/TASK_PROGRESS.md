# 任务进度

> 本文件是所有 AI（Codex / opencode / DeepSeek Harness）共享的任务状态追踪。
> **规则**：每次开始/结束任务都要更新此文件。状态用「进行中 / 完成 / 阻塞 / 等待确认」。
> 涉及文件尽量写具体路径，方便判断是否有冲突。

| 任务ID | 描述 | 工具 | 状态 | 涉及文件 | 最近更新 |
|--------|------|------|------|----------|----------|
| T-000 | 初始化多AI协作规范 | opencode | 完成 | AGENTS.md, .agents/skills/, wiki/* | 2026-09-05 |
| T-001 | 7项UI/交互改进：主页新建分类入口、侧栏把手样式与拖拽跟手、纯模糊背景、待办行内编辑、提醒选择器Material3化 | opencode | 完成 | ui/HomeScreen.kt, ui/TodoScreen.kt, notify/QuickCaptureService.kt, app/build.gradle.kts | 2026-09-05 |
| T-002 | 5项UI/交互改进：把手白色75%不透明度、虚化去纹路、待办光标定位+回车子待办、左滑删除改滑出按钮、废纸篓长按多选+设置页最近删除入口 | opencode | 完成 | notify/QuickCaptureService.kt, ui/TodoScreen.kt, ui/TrashScreen.kt, ui/SettingsScreen.kt | 2026-09-06 |
| T-003 | 左滑真划出删除按钮；待办删除进废纸篓 | opencode | 完成 | ui/TodoScreen.kt, data/Db.kt, data/NoteRepository.kt, NoteViewModel.kt, ui/TrashScreen.kt, notify/QuickCaptureService.kt | 2026-09-06 |
| T-004 | 设置最近删除置顶；废纸篓卡片紧凑去按钮+左侧勾选；待办滑动露圆形红删除键；待办底部弹窗编辑器替代行内/全屏编辑；TodoScreen拆文件 | opencode | 完成 | ui/SettingsScreen.kt, ui/TrashScreen.kt, ui/TodoSwipe.kt, ui/TodoPane.kt, ui/TodoCard.kt, ui/TodoEditSheet.kt, ui/AppRoot.kt, ui/HomeScreen.kt, NoteViewModel.kt | 2026-09-06 |
| T-005 | 删除键对齐修复；废纸篓删清空键+时间改日期时间；前台通知降级静默 | opencode | 完成 | ui/TodoSwipe.kt, ui/TrashScreen.kt, ui/Common.kt, notify/QuickCaptureService.kt | 2026-09-06 |

## 当前进行中的任务（占用检查）

- T-031 | **第六批：侧栏两处"闪" + 待办多选跳动 + 编辑器行距/段落语义**。①通知抢焦点导致输入法收走且不回来 → 在窗口焦点回调里把输入法接回去（强提醒弹出期间 0 次状态变化）。②回车走整窗重挂 → 改就地插行（窗口 id 不变、键盘不掉）。③多选态页头/卡片两套几何导致整列上移 → 钉死页头 32dp、抽出共用 \`TodoCardBody\` 统一边距与槽位（新增 \`todo_selection_verify.py\` 6/6）。④清单行被 48dp IconButton 顶高、勾选框整段居中 → 间距归零、勾选框贴第一行、行高比 1.33（对齐小米笔记反编译实测），条目高 126px→64px、间距 35px→0、长文本仍是同一段。**待确认**：媒体块（图片/录音/链接）拖拽是否也去掉（文本块拖拽已不存在，无死代码） | DeepSeek Harness | 完成 | notify/QuickCaptureService.kt, ui/TodoCard.kt, ui/HomeScreen.kt, ui/EditorScreen.kt, ui/NoteBlockBody.kt, tools/verify/todo_selection_verify.py, wiki/* | 2026-09-17 |

- T-030 | **第五批：点「完成」抽动修复 + 主题换成小米笔记的米黄纸**。①根因 = "窗口重排 + 列表重绘"两次位移叠加（滚动恢复写在 scroll.post{} 里，先画一帧滚动在 0 再跳回）；改成点完成时钉住偏移、在第一次布局（早于绘制）内恢复，并取消防抖中的保存。逐帧验收：修复前 2 次位移（+31/−70px）→ 修复后 0 次。②主题全套色值取自 MIUI 笔记反编译 colors.xml（paper_yellow #FFFAF0 底 + 白卡 #FFFFFF + #FFB21D 强调 + #FFD3/#FFF5EA 容器 + #9D6802 暖棕文字），深浅色、XML 主题（含闪屏）、侧栏悬浮层同步；像素验收全绿。③验收脚本修正把手定位（此前"注入打不开面板"是误判）+ 新增 S4 与 theme_verify.py。**遗留**：④「隐藏保活通知」在 AOSP 13 上被系统重新挂回（撤回调用成功但通知仍在），待真机确认 | DeepSeek Harness | 部分完成 | ui/theme/Theme.kt, notify/QuickCaptureService.kt, res/values*/themes.xml, tools/verify/sidebar_verify.py, tools/verify/theme_verify.py, wiki/* | 2026-09-17 |

- T-012 | 精简开工/发版规则及 CI 修复已完成；actionlint 与 diff 检查通过，已推送 main。用户优先节省 token，APK 补发由 Actions 34243422160 运行，暂不持续轮询；代码文件占用已释放。 | Codex | 完成 | AGENTS.md, .agents/skills/pre-work-check/SKILL.md, .github/workflows/release.yml, wiki/TASK_PROGRESS.md, wiki/DECISIONS.md, wiki/CHANGELOG.md | 2026-09-08 |

> 开始新任务前先看这里：若有其他 AI 正在处理与你重叠的文件，先停下询问用户。

- T-013 | 备份核心（导出/导入含附件）+ 云同步设计 v2 + GLM 任务书。**代码占用已释放**：`backup/` 6 个文件完成且验证（单测 27 + 真机端到端 2）。**下一步交 GLM 做 UI 接线 + WebDAV**，见 `wiki/GLM_TASK_BACKUP_SYNC.md`。 | ZCode | 核心完成/待 GLM 接线 | backup/*, data/NoteRepository.kt, data/Db.kt, wiki/SYNC_DESIGN.md, wiki/GLM_TASK_BACKUP_SYNC.md | 2026-09-13 |
- T-015 | 任务A备份UI接线：设置页"备份与恢复"栏，SAF导出/导入+预览确认，ViewModel BackupState。**已完成并验收**：编译+单测55+connected 2全绿；模拟器换机往返（导出→pm clear→导入，置顶/分类/废纸篓/图片全恢复）、重复导入幂等（跳过7不重复）、两种坏zip可读报错不崩溃、integrity_check ok。**代码占用已释放**。下一步：任务B WebDAV（见 GLM_TASK_BACKUP_SYNC.md） | ZCode | 完成 | NoteViewModel.kt, ui/SettingsScreen.kt, core/DateFormats.kt, wiki/TASK_PROGRESS.md, wiki/CHANGELOG.md | 2026-09-13 |
- T-016 | 三处UI修复：①侧边栏待办新增先只显示内容行、回车才出标题行 ②待办编辑中点空白改保存退回列表（不再收起整个面板到桌面）③笔记正文 BasicTextField 补 textStyle=bodyTextStyle（输入文字与"开始书写或"占位大小/位置对齐）。**已完成并验收**：编译+单测55全绿；模拟器 dump/sqlite 参数化验证——新增仅1个EditText（无标题行）、首按Enter出标题行+插入新行+焦点转移、sqlite确认保存（默认标题待办清单+子项）、编辑态点空白面板保持拉出且空草稿不落库、非编辑态点空白仍收起到把手 | ZCode | 完成 | notify/QuickCaptureService.kt, ui/EditorScreen.kt, wiki/TASK_PROGRESS.md, wiki/CHANGELOG.md | 2026-09-13 |
- T-014 | 结构整理与性能：日期格式化收敛、列表瀑布流缓存、搜索防抖、DB v7 复合索引、refresh 竞态修复、换机迁移含附件 | ZCode | 完成 | core/DateFormats.kt, ui/HomeScreen.kt, ui/Common.kt, NoteViewModel.kt, data/Db.kt, res/xml/data_rules.xml | 2026-09-13 |
- T-006-A | A批：待办行末光标+侧栏原地编辑+长按多选排序；B批：单条静默通知+虚化去纹路 (v1.2.12已发) | opencode | 完成 | ui/TodoEditSheet.kt, ui/TodoCard.kt, ui/TodoPane.kt, ui/HomeScreen.kt, NoteViewModel.kt, data/Db.kt, data/NoteRepository.kt, notify/QuickCaptureService.kt, notify/ReminderReceiver.kt | 2026-09-06 |
- T-006-C | C批：笔记富编辑器（日期YMD/置顶移时间栏/H1-H3/工具栏跟键盘/录音/行内勾选/图片插光标/三点菜单）。**2026-09-16 用户确认收掉**（原为只看截图借鉴、效果差，改为对着反编译实现写）；块模型内核已由 T-020 铺路，UI 批次待开工 | DeepSeek Harness | 进行中 | ui/EditorScreen.kt, ui/NoteCard.kt, data/Models.kt, data/Db.kt, core/RichDoc.kt, core/LegacyBody.kt | 2026-09-16 |
- T-007 | 待办弹窗跟随输入法同步升降（弹窗与键盘渐入渐出同动）(v1.2.13已发) | opencode | 完成 | ui/TodoEditSheet.kt, app/src/main/AndroidManifest.xml | 2026-09-06 |
- T-008 | 侧栏均匀暗化去噪点+把手加灰+侧栏卡片原地编辑+长按多选对标图2 (v1.2.14已发) | opencode | 完成 | notify/QuickCaptureService.kt, ui/TodoPane.kt, ui/TodoCard.kt | 2026-09-06 |
- T-009 | 自查返工：hideKeyboard空转+暗罩太薄+选中米黄太浅+手柄太淡 (v1.2.15已发) | opencode | 完成 | notify/QuickCaptureService.kt, ui/TodoCard.kt | 2026-09-06 |
- T-010 | Android16模拟器全量实测+修复8处bug（弹窗闪退/侧栏新建待办/速记持久化/输入回滚/FAB卡死/拖拽闪回/通知id/时间文案）(v1.2.16已发) | ZCode | 完成 | ui/TodoEditSheet.kt, ui/TodoPane.kt, ui/TodoSwipe.kt, ui/TodoCard.kt, notify/QuickCaptureService.kt, notify/ReminderReceiver.kt, notify/ReminderScheduler.kt, MainActivity.kt, ui/SettingsScreen.kt, app/build.gradle.kts | 2026-09-07 |
- T-017 | 规范实施 A0–G：《PureNote 技术选型、实现规范与验收方案》分阶段落地（分支 `spec/rebuild-a-h`，12 个提交）。**已完成并验收**：单测 164、设备 48、崩溃注入 6 次强杀全绿；设备实测覆盖首页崩溃/升级锁库/图片被毁三处原始故障的修复、v9 迁移、升级接管既有库、自动备份落地。**遗留**：H（云能力）需用户决定是否推翻"不申请网络权限"的产品承诺；性能预算需真机复测（模拟器噪声 ±70%）。详见 wiki/SPEC_REBUILD_REPORT.md | DeepSeek Harness | 完成 | 见 CHANGELOG 同条目 | 2026-09-15 |
- T-018 | **规范阶段 H（云能力）**：INTERNET 权限 + `sync/` 包（RemoteTransport/WebDavTransport/Keystore 凭据/BackupSyncEngine）+ 设置页「云同步」栏 + 设备端到端（真实 wsgidav）。**代码占用已释放**。首版只上传完整备份包：不双向同步、不回读覆盖本地、不删远端旧包。未做：限流退避、S3 第二驱动、E2EE（见 SPEC_REBUILD_MAPPING 附录 B.3） | DeepSeek Harness | 完成 | sync/*, ui/CloudSyncSection.kt, ui/SettingsScreen.kt, NoteViewModel.kt, data/NoteRepository.kt, backup/BackupIo.kt, AndroidManifest.xml, res/xml/network_security_config.xml, wiki/SPEC_REBUILD_MAPPING.md | 2026-09-16 |
- T-011 | 多选UI对标小米实机（三条杠+浅灰）；把手深灰50%；侧栏点空白保存并收起。**待办：T-006-C 笔记富编辑器批次仍未开工**（工具栏随键盘/录音/行内勾选/图片插光标/H1-H3/时间栏YMD/三点菜单精简） | ZCode | 完成 | ui/TodoCard.kt, notify/QuickCaptureService.kt, app/build.gradle.kts | 2026-09-07 |
- T-019 | 脑图（思维笔记）第一批：新增 `feature/mind` 纯逻辑层 —— 树模型与不可变操作（插子级/同级/父级、删除、拖拽换父含环检测、折叠）、JSON 编解码（容错 + 字符保真）、分层整齐树布局（同层不重叠/父节点居中于子节点块/折叠裁剪）。**借鉴小米笔记思维笔记的节点模型与交互语义，代码为 Kotlin 重写**，不含任何小米代码或资源。未接入 UI 与数据层：下一步 NoteKind.MIND + 编辑器接线。与 T-006-C（笔记富编辑器，待开始）无文件重叠，但同属编辑器范畴，优先级待用户确认。 | DeepSeek Harness | **第一批完成**（33 项单测全绿）。**2026-09-16 用户定方案：脑图内容改为新建表（不用 body 存 JSON）**，故 MindCodec 后续仅用于备份导出；表结构与 UI 接线待开工 | app/src/main/java/com/purenote/local/feature/mind/*, app/src/test/java/com/purenote/local/feature/mind/* | 2026-09-16 |
- T-020 | **富文本编辑器升级第一批：块模型内核（body_format_version = 3）**。借鉴小米笔记富文本编辑器的扁平块定义（BlockType text/todo/item/image/sound/link + fragments 行内片段 + checked/sizeLevel/textAlign）与其 note-parser 的解析-序列化-自愈结构，代码为 Kotlin 重写。新增 `core/RichDoc.kt`（块模型 + 纯函数操作：插入/删除/拖拽排序/拆块/并块/行内样式区间开关/清单进度/标题推导）、`core/RichDocCodec.kt`（容错编解码）、`core/LegacyBody.kt`（旧标记文本 ↔ 块文档双向，解析复用 NoteMarkup 以保证与线上渲染逐字一致）。回归亮点：`13800138000. 张三` 这类超长序号行仍按正文处理，覆盖历史首页崩溃场景。**本轮只做内核，数据库与 UI 未动**：用户选定「一次性全库迁移到 v3」，迁移是下一步 | DeepSeek Harness | 第一批完成 | core/RichDoc.kt, core/RichDocCodec.kt, core/LegacyBody.kt, app/src/test/.../core/RichDoc*Test.kt, app/src/test/.../core/LegacyBodyTest.kt | 2026-09-16 |
- T-021 | **富文本编辑器第二批：数据库迁移到 v3 块文档（DB_VERSION 9→10）**。`data/Db.kt` 新增 `< 10` 迁移分支（逐行搬运 + 单行容错：转换失败的行保持旧版本号，读取端仍能解析）；并入 `BODY_FORMAT_BLOCKS = 3` 与 `COL_BODY_FORMAT`；写入路径**显式**写 `body_format_version`（此前只靠 DDL 默认值，是漏标版本的根源），全新安装默认值同步为 3。新增唯一格式闸口 `core/NoteBody.kt`（按行内版本解码 + 双向格式嗅探 + v1 私有标记先过 `migrateBodyV1toV2`）；`NoteRepository` 读写改走闸口；`BackupCodec` 导入把任意来源版本统一规范化成 v3。设备测试 `SchemaMigrationTest` 同步更新（v8→v10、v1→v10、全新安装三条用例断言改为 v10 + 块文档）并新增 v9→v10 专项（文本/清单/已升级三类行 + 幂等复开）。单测 284 全绿。**设备验证已通过（实验：am instrument 直连模拟器 emulator-5554 / SDK 33）**：`SchemaMigrationTest` **4/4 OK**（v8→v10 / v1→v10 / 全新安装 / v9→v10 幂等）；新增 `BodyFormatWritePathTest` **3/3 OK**（磁盘原始行断言：文本笔记存 v3 块文档且读回逐字一致、清单笔记落成 TODO 块且勾选保留、`saveExisting` 修改路径同样写 v3）；`sqlite3` 直查真机库确认 `PRAGMA user_version = 10`、`notes.body_format_version DEFAULT 3`、表结构完整。**遗留作废**：用户决定「当新程序做、不背历史数据包袱」，故备份跨版本兼容（旧 APK 读新包）不再处理 | DeepSeek Harness | 第二批完成（含设备验证） | data/Db.kt, core/NoteBody.kt, data/NoteRepository.kt, backup/BackupCodec.kt, app/src/test/.../core/NoteBodyTest.kt, app/src/androidTest/.../data/SchemaMigrationTest.kt, app/src/androidTest/.../data/BodyFormatWritePathTest.kt | 2026-09-16 |
- T-022 | **任务②-2.1 正文块渲染（UI）**：新增 `ui/NoteBlockBody.kt`，与 `TextNoteBody` **保持完全相同的参数契约**（value/onChange/onCursor/cursorRequest），故 `EditorScreen` 只换一行调用，保存/工具栏/插图/IME 逻辑全不动、可一行回退。实现：块列表渲染（H1-3 字号、TODO 勾选框、•/n. 列表、引用、首行缩进、图片块降采样加载、录音块、链接块）、回车拆块并按类型续接、段首退格并块（走 onPreviewKeyEvent，因为此时文本无变化）、清空即并入上一块、新块真正 requestFocus。**实测修掉两个自身 bug**：①输入时未同步 doc 导致回车拆块位置错位（表现为 alpha/beta 挤成一行）②FocusRequester 从未调用 requestFocus。**设备验证（adb 直连 emulator-5554）**：新建笔记输入 alpha→回车→beta，界面呈现两个独立块、字数 10 字，sqlite3 直查库内为 kind=0 / body_format_version=3 / 两个 TEXT 块。旧 TextNoteBody 保留为回退参考 | DeepSeek Harness | 2.1 完成（设备验证通过） | ui/NoteBlockBody.kt, ui/EditorScreen.kt | 2026-09-16 |
- T-023 | **任务②-2.2 图片/录音改为真正的块插入**。新增 `core/BlockEdit.kt`（纯函数 + 12 项单测）：`lineStarts`/`blockIndexAtOffset`/`offsetIn` 全局偏移↔块映射、`insertEmbedAt`（**空行就地占位**，沿用旧 `insertImageLineAtCursor` 的语义；插入后其后必有可输入文本块，连续插图会补空块）、`insertEmbedMarkup`（Markdown 进出，供编辑器直接调用）。`LegacyBody` 暴露 `encodeLine` 供逐块算偏移。`EditorScreen` 四处入口（拍照/相册/录音/手写画板）统一改为 `insertEmbedAsBlock()`，插入位置按**块**计算并把光标移到新落点，不再直接拼 `![](...)` 文本行。去掉 `NoteBlockBody` 里那行 alpha=0 的提示文字（看不见但**进了无障碍语义树**）。**设备验证（adb 直连）**：sqlite3 造一条含 IMAGE 块的 v3 笔记 → 首页卡片显示「［图片］」→ 编辑器内图片块渲染为 964x840、其下存在可输入块 → 在该块输入 after-img → 库内结构为 `[TEXT]图片块验证 / [IMAGE]img_seed.png / [TEXT]after-img` | DeepSeek Harness | 2.2 完成（设备验证通过） | core/BlockEdit.kt, core/LegacyBody.kt, ui/EditorScreen.kt, ui/NoteBlockBody.kt | 2026-09-16 |
- T-029 | **待办页两条反馈**：①左滑露删除键时勾选栏直接消失（滑动进度驱动，主/子条目都收起）②多选态按钮挪位——退出多选的 X 到"待办"大字上方、右上角全选替换设置齿轮（退出恢复）；顺带补齿轮的 `contentDescription`。**已完成并设备验证**：`todo_feedback_verify.py` 10/10 PASS | DeepSeek Harness | 完成 | ui/TodoSwipe.kt, ui/TodoCard.kt, ui/TodoPane.kt, ui/HomeScreen.kt, tools/verify/todo_feedback_verify.py, wiki/* | 2026-09-17 |
- T-028 | **用户四条反馈修复（小米 12S 体验）**：①保存状态改右下角悬浮（不再顶动正文）②清单回车 = 新开一条勾选栏 ③清单笔记补焦点上报、五键工具栏可用（勾选键加新行，插图/录音/手写进附件条）④宫格首页换 LazyVerticalGrid 单页滚动。**已完成并设备验证**：`feedback_verify.py` 11/11 PASS | DeepSeek Harness | 完成 | ui/EditorScreen.kt, ui/HomeScreen.kt, tools/verify/feedback_verify.py, wiki/* | 2026-09-17 |
- T-027 | **取消文本块拖拽 + 修 API33 工具栏不可达 + MiCheckbox 无障碍语义 + 规模验收**。文本块长按不再触发拖拽排序（只保留图片/录音/链接块），长按回归选字；工具栏显隐并上"正文获得焦点"信号，修掉 `adjustResize` 设备（API<35）上样式键完全点不到的问题；`MiCheckbox` 改 `toggleable(role = Role.Checkbox)` 补上勾选框语义。**已完成并设备验证**：新增 `scale_verify.py` 大数据量纯直连验收，Android 13/16 各 42 项 0 失败；工具栏 10/10、块编辑 6/6、脑图 19/19、拖拽口径 6/6；单测 298 | DeepSeek Harness | 完成 | ui/NoteBlockBody.kt, ui/EditorScreen.kt, ui/TodoCard.kt, core/RichDoc.kt, tools/verify/*, wiki/* | 2026-09-17 |
- T-026 | **任务④ 脑图接入数据层与界面（第一批）**。`NoteKind.MIND`（kind=2）+ `Note.mind`；Repository 按 kind 分派（脑图走 MindCodec，卡片投影成大纲）；SaveCommand/ViewModel 带 mind。`ui/MindEditor.kt`：导图（整齐树布局 + 贝塞尔连线，节点是真实 Composable，可被无障碍树看到）+ 大纲视图 + 节点操作（加子级/加同级/改文字/折叠/删除）+ 点空白取消选中；首页「新建脑图」入口；脑图根节点即标题。**已完成并设备验证**：`mind_verify.py` 15/15（几何/加子级/折叠/大纲/新建全链路 kind=2），另三套回归全绿；单测 298。**过程中修掉两个会坏数据的 bug**（saveExisting 把 kind 写成 0/1 → 脑图被降级成文本；备份导入把脑图正文过了块文档闸口 → 树被压成一行）。**第二批已完成**：画布长按拖动换父（含环检测与"换父后自动展开"）+ 字数/分享投影，设备验证 19/19。**未做（下一批）**：双指缩放、导图导出图片、节点拖动时跟手的镜像 | DeepSeek Harness | 第一批完成 | ui/MindEditor.kt, ui/EditorScreen.kt, ui/HomeScreen.kt, ui/NoteCard.kt, data/*, feature/notes/*, backup/BackupCodec.kt, tools/verify/mind_verify.py, wiki/TASK_PROGRESS.md, wiki/CHANGELOG.md | 2026-09-16 |
- T-025 | **任务②-2.4 工具栏直接操作光标所在块 + 删除标记桥**。正文以 `RichDoc` 为唯一形态贯穿「编辑器 → ViewModel → SaveCommand → Repository → 落库」：`Note` 新增 `doc`（`body` 降级为列表卡片/通知用的只读投影），写入走 `NoteBody.encode(doc)`。样式键改块属性：新增 `RichDoc.toggleHeadTag(blockId, type, ordered)`（ITEM 用 number 区分 • 与 1.；有序接续上块序号）。**删除**：`TextNoteBody`、`MarkupOverlay`、`transformNoteText`、`enterInheritIntercept`、`backspaceIntercept`、`NoteMarkupVisualTransformation`、`insertEmbedMarkup`、全局偏移三件套及其单测（编辑器 −660 行）。**已完成并设备验证**：`block_toolbar_verify.py` 10/10（API 36）、`block_edit_verify.py` 5/5（API 33）、`block_drag_verify.py` 6/6 回归；单测 296 全绿。**遗留（与本次改动无关，待定）**：API 33 上工具栏不显示——`adjustResize` 下窗口被压缩，`WindowInsets.isImeVisible` 为 false（API 35+ 强制边到边才有 IME inset），样式键在 Android 13 上不可达 | DeepSeek Harness | 完成 | ui/EditorScreen.kt, ui/NoteBlockBody.kt, core/RichDoc.kt, core/BlockEdit.kt, data/Models.kt, data/NoteRepository.kt, NoteViewModel.kt, feature/notes/*, tools/verify/block_*_verify.py, wiki/TASK_PROGRESS.md, wiki/CHANGELOG.md | 2026-09-16 |
- T-030 | **"编辑器键入失败"定性：测试取点问题，不是 v1.2.24 回归**。三个编辑器套件（block_edit / block_toolbar / scale）在 v1.2.24 上大面积 FAIL，一度按"da42ac3 引入回归"做 bisect。实测（emulator-5554 与 5556、v1.2.23 与 v1.2.24 表现完全一致）：uiautomator 报的正文块 EditText bounds 比**实际绘制位置**高约 46px（标题行、日期行、勾选框的坐标都是准的，只有正文块行偏），照 a11y 坐标点会点在文字上方空白处 —— 输入法不弹、`input text` 进不去、落库为空。按真实墨迹位置点则：输入框拿到 Press/Release、a11y `focused` 变 true、`mInputShown=true`（键盘弹出）、DB 落库 AAA→AAA12。第二个坑：焦点一落到某块，编辑器会把光标滚进可视区（键盘弹出后视口变矮），先前的坐标全部失效。新增 `tools/verify/editor_tap.py`：用截图的"墨迹行带"定真实位置（a11y 只用来定顺序），每次取点前先收键盘，点完用 a11y `focused` 属性确认落对行（比"键盘弹没弹"准）。复核：block_edit_verify 6/6、block_toolbar_verify 10/10、scale_verify **42 项 0 失败**。规模矩阵剩下 8 项也全是取点/配对问题（不是应用缺陷）：① A4 按类名找勾选框会漏 —— 同一个 MiCheckbox 有的行报 `CheckBox`、有的报 `View`，但 `checkable=true & checked=true` 都在，改成按语义找；② D 用 `|Δy|<70` 配对勾选框，清单行高 64px，于是把**上一行**的勾选框当成这一行的、勾错了行，改成竖直重叠面积取最大；③ C 的光标取点：列表/引用块前面有 28dp 标记槽，拿第一块的 x 去点会点到槽上，改成用目标块自己的左边界；墨迹行带会被字形间的 1~2px 空隙拆成两条，不合并会让"第几行"整体错一位，合并间隔 ≤4px 的相邻带；再把"行带位置"与"a11y 中心 + 0/18/…/90px"都作为候选逐个试、用 a11y `focused` 判定命中 | DeepSeek Harness | 定性完成 / 套件修复 | tools/verify/editor_tap.py, tools/verify/block_edit_verify.py, tools/verify/block_toolbar_verify.py, tools/verify/scale_verify.py, wiki/TASK_PROGRESS.md | 2026-09-18 |
- T-024 | **任务②-2.3 块级长按拖拽排序**。新增 `core/BlockDrag.kt`（纯函数 + 13 项单测）：`blockIndexAt`（长按命中哪一块，空隙归上一块）、`blockInsertIndex`（指针纵坐标 → 插入边界，按悬停块上/下半区决定插前还是插后）、`blockMoveTarget`（边界 → `RichDoc.move` 的下标；返回 null = 顺序不变，调用方据此不写库也不画指示线）、`moveToBoundary`。`ui/NoteBlockBody.kt`：懒列表外包一层 Box 画插入指示线；`Modifier.pointerInput` 用 `awaitLongPressOrCancellation` 取长按（**长按前不碰任何事件**，文本框点击/选区、勾选框、列表滚动照常），越过 `touchSlop` 才进入拖拽并在 **Initial 阶段**吃掉事件；被拖块用 `graphicsLayer` 抬起跟手（zIndex + 阴影 + 1.02 缩放 + 触感反馈），拖到列表上下边缘自动滚动。**与小米的差异（有意）**：小米一律插到悬停块之后（clientY 到顶时被强行设成首块，第一块永远拖不到文首），这里按悬停块上下半区决定插前/插后。**开发中实测抓出并修掉两个自身 bug**：①拿单次事件的位移与 slop 比较——慢速滑动的每条事件只有零点几像素，拖拽永远起不来；②自己累加增量会丢位移，改用指针绝对位置后落点才精确（此前会差一格）。**设备验证：`tools/verify/block_drag_verify.py` 6/6 PASS**（三块夹具 → 长按第三块拖到最前 → 界面顺序 CCC/AAA/BBB → 库内 v3 块文档顺序一致 → 原位拖动顺序不变且 `updated_at` 未变）。**单测 309 全绿**（新增 13）。已知遗留：正文文本框自身的"长按选字"与块拖拽抢同一手势，从文字上起拖会带出系统 Select all/Autofill 浮层（拖拽结束时选区已收回，浮层需下一次触摸才消失）；若要在文字上干净起拖，需改成"从左边缘把手拖"的方案 | DeepSeek Harness | 完成 | core/BlockDrag.kt, ui/NoteBlockBody.kt, tools/verify/block_drag_verify.py, app/src/test/.../core/BlockDragTest.kt, wiki/TASK_PROGRESS.md, wiki/CHANGELOG.md | 2026-09-16 |
